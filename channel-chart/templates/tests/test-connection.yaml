apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "channel-chart.fullname" . }}-test-connection"
  labels:
    {{- include "channel-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "channel-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
