apiVersion: v1
kind: Service
metadata:
  name: {{ include "channel-chart.fullname" . }}
  labels:
    {{- include "channel-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "channel-chart.selectorLabels" . | nindent 4 }}
